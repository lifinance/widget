---
"@lifi/widget": minor
---

Apply a navigation tab's `sdkConfig` (including `apiUrl`) when the tab is active, so a tab can route through a dedicated backend endpoint. Tab switching is locked while a route is executing.
