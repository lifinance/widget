---
"@lifi/widget": patch
---

Show the "Bridge completed" receipt row for bridges whose destination transaction hash is not a `0x`-prefixed EVM hash (e.g. Lighter), building the explorer link from a chain-specific path override.
