---
"@lifi/widget": patch
"@lifi/widget-checkout": patch
---

Keep the unsupported-receiver block consistent across the limit order and checkout flows, so a route that cannot honour a required receiver stays blocked everywhere.
