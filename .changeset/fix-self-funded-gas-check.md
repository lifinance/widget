---
"@lifi/widget": patch
---

Stop showing the insufficient gas warning on Stellar when the route bridges enough XLM to cover the destination step, so a receiver with an empty XLM balance no longer sees a false warning.
