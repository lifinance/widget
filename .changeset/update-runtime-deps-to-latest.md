---
"@lifi/widget": patch
---

Update `react-intersection-observer` to `^11.0.1`.

`viem` also moves to `2.56.0` in the lockfile. The declared range stays at
`>=2.52.0`, so a consumer on any 2.5x release still resolves a single copy.
