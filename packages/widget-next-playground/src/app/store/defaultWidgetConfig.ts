import { WidgetConfig } from '@lifi/widget';

export const defaultWidgetConfig: Partial<WidgetConfig> = {
  appearance: 'auto',
  theme: {
    shape: {
      borderRadius: 123,
    },
  },
} as WidgetConfig;
