import { domMax } from 'motion/react'

/**
 * Motion features in a standalone module so they can be split into their own chunk.
 *
 * @see {@link https://motion.dev/docs/react-reduce-bundle-size}
 */
export default domMax
