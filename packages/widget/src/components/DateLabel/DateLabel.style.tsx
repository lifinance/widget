import { Box, styled, Typography } from '@mui/material'

export const DateLabelContainer = styled(Box)({
  display: 'flex',
  justifyContent: 'space-between',
})

export const DateLabelText = styled(Typography)({
  fontSize: 12,
  fontWeight: 500,
})
